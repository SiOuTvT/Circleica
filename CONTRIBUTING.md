# 贡献指南

感谢你关注 Circleica。下面说明如何在本仓库里安全地工作。

## 开发流程

1. Fork / 克隆后，按 `docs/DEVELOPMENT.md` 完成环境搭建与数据库迁移。
2. 从 `main` 切出特性分支开发。
3. 提交前确保本地门禁通过：`npx tsc --noEmit`、`npm run lint`、`npm run test`。
4. 生产构建与端到端测试建议在 CI 环境跑（本地 Windows 沙箱可能因安全删除机制拦截 `.next` 清理，属环境限制，不是代码问题）。

## 代码约定

- 公共页面优先用 Server Component；只有真正需要交互的地方才用 Client Component。
- 数据获取走 Prisma，查询用 `select` 裁剪字段，列表必须分页。
- 所有写接口通过统一的 API 边界（`src/lib/api-handler.ts`），入参用 Zod 校验。
- 状态变更接口受 CSRF 同源校验保护；限流按路由维度配置。
- 不把密钥、内部错误信息、堆栈暴露给客户端。

## 提交与 PR

- 提交信息写清楚「做了什么 / 为什么」，避免无关改动混在一起。
- 涉及数据库结构变动必须同时提交 Prisma 迁移（`npx prisma migrate dev`）。
- 涉及安全、权限、资源访问的改动请在 PR 描述里说明影响面。
- 不要提交 `.env`、构建产物、截图或一次性诊断脚本。

## 文档

正式文档放在仓库根目录与 `docs/`；历史审计过程材料已归档到 `archive/`，不在 GitHub 门面位置。

## 行为准则

请就事论事、尊重社区。涉及同人作品版权与创作者权益的内容请谨慎处理。
