# 宝塔面板 Nginx 配置参考 - 解决 HTTP 431 错误
# 在宝塔面板：网站 → 设置 → 配置文件，在 server {} 块内添加以下内容

# 增大请求头缓冲区（NextAuth JWT cookies 较大，Node.js 默认 8KB 限制会触发 431 错误）
large_client_header_buffers 4 16k;
client_header_buffer_size 16k;

# 可选：增大代理请求缓冲区（防止 Next.js API 响应头过大）
proxy_buffer_size 16k;
proxy_buffers 4 16k;