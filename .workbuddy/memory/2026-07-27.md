# 2026-07-27

## 修复 next/image 图床白名单导致 Galvelica 首页崩溃
- 现象：访问 `/galvelica` 抛 `Invalid src prop (https://res.cngal.org/...) on next/image, hostname "res.cngal.org" is not configured under images`——编辑器精选(editor-pick)组件里 CnGal 封面图床未加入白名单，`next/image` 在 SSR 阶段直接整页崩。
- 根因：`next.config.ts` 的 `images.remotePatterns` 缺 Galvelica 各源图床域名。
- 修复：按 DB 实测封面域名（`SELECT host FROM coverImage`）补全白名单——实际入库 5 个域名中只有 `t.vndb.org` 已配，其余 `tucang.cngal.top`(648)/`res.cngal.org`(14)/`shared.cdn.queniuqe.com`(3)/`media.st.dl.eccdnx.com`(1) 全缺；并前瞻加 Steam/Bangumi/月幕 图床防以后踩坑。
- 验证：重启宿主 netns dev server（`nohup ... & disown` + `dangerouslyDisableSandbox`，netstat 确认 `:3000 LISTENING`）；`/galvelica` HTTP 200、首页零 `unconfigured-host` 字样；真实 `res.cngal.org`/`tucang.cngal.top` 封面经 `/_next/image` 优化端点均返回 200 image/png。
- 注意：`res.cngal.org/.../default/app.png` 是 CnGal 默认占位图，部分作品把该默认图当 cover 存了（数据质量小瑕疵，非本次范围，未处理）。
- 没碰 C 盘；临时诊断脚本已清理；记忆已补长期约定。

## 部署战略转向预检（2026-07-27 晚）
- 用户通知：本地电脑内存受限跑站会崩，今后转 **Coolify 部署到国内便宜测试服务器**，不再本地迭代；要求部署前多查几遍、一次性解决。
- 预检结论（已读 Dockerfile/compose/entrypoint/.env.example/next.config/health 路由）：仓库对国内部署基础友好（npm/apt 已切国内镜像；standalone 已开；/api/health 存在；entrypoint 自带 migrate deploy）。
- 暴露的阻断级风险：① 基础镜像 `node:20-bookworm-slim`/`postgres:16-alpine` 来自 Docker Hub，国内服务器拉取可能失败→需配镜像加速器或预拉；② `NEXTAUTH_SECRET` 未显式设在容器内不挂卷→重建掉登录；③ 国内服务器直连 VNDB/Steam ingest 可能不稳→建议本地已 populate 库 `pg_dump` 迁过去；④ 低内存机 `next build` 2G 堆可能 OOM→需加 swap/降内存。
- 已记入 MEMORY.md「部署策略」小节。待用户拍板：填数方案（迁本地库 vs 服务器 ingest vs 空库）、访问地址（域名+SSL vs 纯 IP）、服务器能否拉 Docker Hub。

## 用户补充修正（回答预检三问，2026-07-27 晚）
- Coolify **已实际部署过**，当前是 push→Coolify 重建→贴报错修 的循环，非首次部署；地址用 Coolify 域名，因国内需备案经 Cloudflare 之类中转（代理在应用前）。
- 本地库数据质量用户存疑（『一言难尽』），迁库方案暂不成立，填数待另查。
- Docker Hub 拉取靠重试终能成功，非硬阻断；仍建议配镜像加速器。
- 据此修订 MEMORY.md「部署策略」：补 NEXTAUTH_URL 必设(否则 localhost 回调致登录失效)、Cloudflare 代理协议头、本地库不可盲目迁。
- agent 已生成强 NEXTAUTH_SECRET 供贴入 Coolify env（避免容器重建掉登录）。

## 用户二次修正 + 协作边界（2026-07-27 深夜）
- 用户明确：db/env/swap 在服务器上**早已配好**，部署已成功且内容已测——此前预检把 ②④ 当阻断项是过度假设，已收回。Docker Hub 拉取用户实测重试可成。
- **协作模型定稿（2026-07-27 深夜）**：用户负责部署/测试/Git 提交/服务器与 Coolify/Cloudflare 基础设施；agent 负责写代码与修网站（用户授权「帮我修网站写网站」）。agent 不做 git/部署/服务器/改 Docker-infra（除非用户要求）。改完说明改动，用户部署测试；报错贴回 agent 再修。共享项目：大改动先知会，常规修 bug/写功能直接做。本次清掉我遗留的临时 `diag.ps1`（已 rm）；`dev-run.log` 是 dev 日志非代码，提醒别提交。
- 结论：不需主动预检/只读自检，用户说「等报错即可」。之后流程=用户部署测试→报错贴我→我给根因+改代码（git/部署由用户做）。

## Galvelica 同人收录范围修正（用户多次反馈「资料不全/不像同人/多源没用」）
- 用户核心纠正：「同人」= 个人或小社团自主制作。VNDB 生产者分 ng(业余社团)/in(个人)/co(公司)，**VNDB 无 doujin 布尔字段**，只能靠生产者类型判定（直连 Kana API 验证：请求 `fields:"doujin"` 报 "Field 'doujin' not found"）。
- 旧实现错在哪：`scripts/ingest-vndb.ts` 默认过滤 `["developer","=",["type","=","ng"]]` 只收 ng，且防御校验显式 `continue` 跳过 `in`(个人) 与 `co`(公司)。→ 个体创作者全漏；成名同人社团（TYPE-MOON/âge/Nitroplus，VNDB 标 co）全漏。直连验证：07th Expansion=ng（寒蝉本应在库，缺是因摄入不完整/搜索问题待查）；TYPE-MOON=p6(co)/âge=p4(co)/Nitroplus=p42(co) 均被跳过。
- 多源没发挥：部署批量摄入实际只 VNDB(ng)+CnGal+Steam；月幕 YmGal 默认被闸门关掉（中文同人最大缺口）；Bangumi 在白名单但**无批量摄入脚本**（仅按需搜索）。
- 修复（代码已落，tsc/lint 0 error）：① `ingest-vndb.ts` 默认过滤改为 `["or", developer.type=ng, developer.type=in, developer.id∈白名单]`；co 白名单种子 `["p6","p4","p42","p27","p240","p336","p143","p1040","p65","p13"]`（TYPE-MOON/âge/Nitroplus/minori/OVERDRIVE/Frontwing/130cm/KeroQ/Circus/Navel），可用 `GALVELICA_DOUJIN_CO_IDS` 环境变量追加；防御校验同步放开 ng+in+白名单co。② `doujin-gate.ts` 把 `YMGAL` 加入 `DOUJIN_CURATED`（默认放行）。③ `ingest-entrypoint.sh` 月幕改为默认抓取（保留 `GALVELICA_SKIP_YMGAL=1` 跳过）。VNDB 复合 OR 过滤语法已直连验证可用（`id` 不能传数组，必须用多个 `id` 分支 or）。
- 待用户（部署侧）：① 管理员 `net start postgresql-x64-16` 起本地库，让 agent 查库确认寒蝉等是否在、摄入缺多少；② 重填需 `GALVELICA_INGEST_RESET=1`（过滤变了断点不适用）全量 re-ingest；③ 验证前台搜索能搜到同人作。
- 遗留多源缺口（下一步建议）：补 `ingest-bangumi.ts` 批量摄入脚本，使资料馆批量多源真正含 Bangumi。

## 用户定稿「同人 Galgame 完整定义」+ 国内源排除铁律（2026-07-27 深夜，重大修正）
- 用户给出可固化为知识库的三类定义：**①纯正同人**=个人(in)/无注册社团(ng)，仅同人渠道分发（核心库）；**②同人系公司**=早年同人社团、后期注册公司的厂商全部作品（同人衍生拓展库，对外须与①分两类展示、明示定义区别）；**③原生纯商业公司**（柚子社/Key/5pb/Sphere，创立即商业）永久排除。
- 站点采用 **rule 2（ng+in+同人系公司白名单）**，非 rule1（只 ng+in）/rule3（全收）。与上一轮已落的 co 白名单一致。
- **数据源权威铁律（最高优先级）**：优先全球公认、国际圈通用、海外认可平台；**月幕(YmGal)/CnGal/KunMoe 全部排除**——不接 API、不批量爬、不依赖。原因：深度绑定国内同人/汉化/社团圈子，用户要保持独立中立、不碰国内圈人际关系。→ 这推翻上一轮把 YmGal 加进白名单的决定。
- **代码回退（本轮回退，tsc/lint 0 error）**：① `doujin-gate.ts` 的 `DOUJIN_CURATED` 从 `[VNDB,BANGUMI,CGL,STEAM,YMGAL]` 改为 `[VNDB,BANGUMI,STEAM]`（移除 CNGL+YMGAL）；② `ingest-entrypoint.sh` 的月幕默认抓取块改为排除注释（gate 已挡）。`ingest-vndb.ts` 的 co 白名单（rule2）保留不动。
- **待用户拍板的 4 个分叉**（已用 AskUserQuestion 问）：① Bangumi 是否纳入（中文源边界模糊）；② EGS/ErogameScrape（日本权威 galge 库，但中国 IP 墙，国内服务器直连会被墙，要不要实现）；③ 已入库的 CnGal/YmGal 数据怎么处理（清空/保留不补/标记）；④ 分类展示字段（纯正同人 vs 同人系公司商业作）现在就加 `doujinCategory` 还是以后做。
- 关联待办（依赖答案）：若清空 CnGal/YmGal 数据→顺手从 `next.config.ts` remotePatterns 删其图床域名 + 从 `fusion.ts` FUSION_TABLE 去其源行（勿提前删，否则已有封面 500）。
- MEMORY.md 已整体精简并固化「Galvelica 同人定义 & 数据源铁律」小节（最高优先级）。

## 收尾状态确认（2026-07-27 延续，agent 自验）
- 代码已落（任务 #85–#88、#90 完成）；本轮回合仅做状态自验：source-only `tsc --noEmit` **0 error**（仅 `.next/dev/types/validator.ts` 2 个错，属 next dev 中断导致的生成文件损坏，`next build` 会重建，非源码问题）；lint 0 error 前轮已验。
- 闸门确认：`doujin-gate.ts` `DOUJIN_CURATED=[VNDB,BANGUMI,STEAM]`，`gateAllowsSource(YMGAL/CNGL)` 返回 false → 国内源在入库口被挡，铁律功能层面满足。
- `cngal.ts`/`ymgal.ts` 适配器仍物理存在但仅注册、闸门挡、永不被调用；若要字面满足「不接 API」可物理删除（待用户拍板，agent 未擅删）。
- **任务 #89 起库验证阻塞**：本地 Postgres 未起（`netstat` 无 5432 监听），起库属用户 infra 任务，agent 不代启。待用户 `net start postgresql-x64-16` 后跑 `qa-ingest`（确认寒蝉等在库/缺多少）+ `PURGE=1 npm run galvelica:purge-domestic`（清 CnGal/YmGal）。
- 部署清单（Coolify，用户侧）：① `prisma migrate deploy`（应用 WorkCategory 迁移）② `GALVELICA_INGEST_RESET=1` 全量 re-ingest（过滤已变 ng+in+co白名单）③ `PURGE=1 npm run galvelica:purge-domestic`（清国内源数据，须在站点服务其封面前提早跑，因 next.config 已去其图床域名）④ 可选 `GALVELICA_ENABLE_EGS=1`（需服务器日本出口代理）⑤ 前台验收「纯正同人 / 同人系公司」徽章。

## 部署就绪自检（用户部署前，agent 排雷）
- 排查三个会致部署失败的雷，全部排除：
  1. `Dockerfile` 第24-26行 `RUN npx prisma generate` → 带 doujinCategory 的 Prisma 客户端在构建期生成；第98行 prisma 拷入 runtime。✓
  2. `.dockerignore` 第13行 `.next` 已排除 → 本地损坏的 `.next/dev/types/validator.ts` 不会进构建上下文，`next build` 在容器内重建 `.next`，不受那 2 个生成文件错误影响。✓
  3. `migrate-entrypoint.sh` 第42行 `npx prisma migrate deploy --schema=./prisma/schema.prisma` → 新迁移（20260727090000_work_doujin_category）部署时自动应用，doujinCategory 列会被建。✓
- 残留提醒用户：① 若服务器内存 <2GB，`next build` 的 `--max-old-space-size=2048` 可能 OOM（需加 swap/降内存，属 infra）；② 部署后须按序 re-ingest(RESET) + purge(PURGE=1)。
- **更正（2026-07-27 续）**：早前「残留国内源封面会 500」的判断**不成立**——`fusion.ts` 第75行 `coverImage` 优先级 `[VNDB,STEAM,BANGUMI,MANUAL,DLSITE]` 不含 CNGL/YMGAL，`Work.coverImage` 永不从国内源取，故删其源行不会留下指向已移除图床域名的封面，purge 脚本无需改。用户部署手顺已给：Coolify 构建/迁移零手动；数据回填走 Coolify 容器 Terminal 或 `docker exec -it <web> sh` 跑 `GALVELICA_INGEST_RESET=1 nohup npm run galvelica:ingest-vndb`（后台，慢）后 `PURGE=1 npm run galvelica:purge-domestic`；或 `GALVELICA_INGEST_RESET=1 docker compose run --rm ingest`（独立 ingest 服务，脱离终端）。
- 待用户部署后拍板：① 是否物理删 `cngal.ts`/`ymgal.ts`+清 `index.ts` 注册+删 `package.json` 两条 ingest 脚本（功能已排除，删=字面满足「不接 API」）；② 旧库过宽摄入的非同人作是否要重算清理（re-ingest 只加不删）。
