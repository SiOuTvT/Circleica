# 2026-07-26

- 用户把 Galvelica 启用指令整段甩给独立的「启动 AI」执行本机命令（用户本机跑 DB，沙箱连不上）；该启动 AI 聪明，给自包含命令清单即可，不用手把手。
- **⚠️ 环境认知修正（重要）**：此前 working-memory 记载「agent 环境网络层拦截 127.0.0.1:5432、连不上库」。但 2026-07-26 17:28 实测：`dangerouslyDisableSandbox:true` 下直接 `npx tsx scripts/qa-ingest.ts` 成功连上本地 Postgres 并返回真实数据（总 Work 11,385、VNDB 10,578 + CnGal 807）。说明该拦截本会话不成立——**以后要查库/跑 ingest 直接试，别先入为主推定连不上**。
- **Galvelica 收录质量诊断（真实数据，非猜）**：家 AI 跑完 ingest 后，我实测 `qa-ingest.ts` 输出：总 Work=11,385（VNDB 10,578 / CnGal 807，无跨源合并→total works==total sources）；title/releaseDate/studioName/description 完整度 100%/96.5%/92.9%/77.2% 优秀；孤儿/空title/异常status 全 0；aliases 31.7%（可接受）；**疑似重复组 76（交叉源未合并，次要待查）**；**封面 7.1% 真 bug**——`ingest-vndb.ts` 的 LIST_FIELDS 没请求 `image` 且 `vndb.ts` normalize 未映射 `coverImage`，导致 10,578 条 VNDB 全部无封面（仅 CnGal 807 有图）。已修复：① `vndb.ts` normalize 加 `coverImage: vn.image`；② `ingest-vndb.ts` LIST_FIELDS 加 `image`；tsc 0 error。待家 AI 跑 `GALVELICA_INGEST_RESET=1 npm run galvelica:ingest-vndb` 回填封面（约 40min，幂等）。officialUrl/steamAppId 0% 属预期（未接入 DLsite/Steam 源），非 bug。
- **新增只读质量诊断脚本 `scripts/qa-ingest.ts`**：用于核查 Galvelica ingest 结果的数据质量（沙箱无库、必须由家 AI 在本机跑 `npx tsx scripts/qa-ingest.ts` 贴回输出）。只 SELECT 不改数据。报告：各源 WorkSource 分布、融合字段完整度(title/originalWork/aliases/description/coverImage/releaseDate/studioName 等)、孤儿 Work/异常 status/空 title、疑似重复(同 title+releaseDate)、发售日年份分布+未来年份检测。阈值：cover≥60%/releaseDate≥80%/desc≥50%/孤儿=0/重复组<50/未来年份=0。配合家 AI 报的 VNDB 11,385 + CnGal 806 ≈ 12,000 核对。tsc 0 error。
- **代码层质量审计（已审，不用等数据）**：`ingest-vndb.ts` 的 `ng` 过滤正确（仅开发商 type=ng 同人社团入库，co/in 在入库口 continue 跳过）→ 无商业游戏混入；`ingest-discovery.ts` 走 `gateAllowsSource("STEAM")` + `isVisualNovelApp`（仅 genre 含 Visual Novel/Dating Sim）放行；两套 ingest 均幂等 upsert 不重复。注：STEAM 发现层大概率家 AI 尚未跑（其报数未含 Steam），属正常增量步骤。
- **家 AI 跑通 ingest 并汇报数据**：VNDB 同人（开发商类型 ng 过滤）11,385 条（VNDB 全量 ~37,000 VN 中同人社团约 30%）；CnGal 国产 806 条；合计 ~12,000。家 AI 问「要不要去掉 ng 放宽到商业 VN」→ 用户明确震怒「这是同人站！别问这种问题」。结论：**保持 ng 过滤、绝不放开到商业 VN**，与 Galvelica「只收同人」铁律一致，无需改代码。教训：凡与已确立核心定位（只收同人）相悖的方向性提问属「非问题」，应直接按既定立场拍板、不要反问用户。
- 用户问「这个功能到底是什么」→ 用全景图 + 产品视角说明：Galvelica=同人 VN 资料馆，与 Circleica 资源站互补并存；含 ① 多源数据融合 ② 收录申请闭环 ③ 搜索分治。代码六步全落地，仅差本机建表+回填才有数据。
- 用户担心申请收录带来后台工作量爆炸：核对代码确认 ① backfill 给每个 Work 设 gameId（第100行）→ 回填后申请按钮不出现、零公开流量；② 用户无入口创建任意 Galvelica Work（仅回填/后台手动/未来扩目录）；③ 申请通过只建未发布草稿（融合数据预填），管理是核验非从零建。残留决策=未来扩目录时申请闸门策略（仅登录+同人标签过滤 / 先不加 / 关公开申请），待用户选。
- 用户进一步澄清核心诉求=站子身份必须是「同人」：Galvelica 搜索是站内搜（只查自己的 Work 资料库，不代理搜 VNDB），所以只会返回已入库作品；当前作品 100% 来自 Circleica 已策展的同人 galgame，商业作/非 VN 根本进不了库也搜不到。未来扩目录时必须加「只收同人 VN」硬过滤（VNDB 看同人 circle/免费标记、Bangumi 看同人标签），入库即挡非同人。用户强烈要求：同人站就不能搜到非同人游戏。此「只收同人」应作为 Galvelica 的硬不变式（invariant）写入代码层+架构文档。
- 用户纠正我的轻描淡写：Galvelica 按约定=搜到「所有同人视觉系小说」（整个同人 VN 生态，DB 远大于 Circleica），多数作品不在主站→显示申请收录→回管理页。之前『按钮不出现/零流量』只是当前仅回填的状态，非完整设计；工作量担忧真实。但工作量≠人工调研：作品来自 VNDB/Bangumi 结构化可信源（带同人过滤），申请通过自动建预填 Game 草稿，管理员只点采纳/拒绝。待决=采纳模型：A 自动建草稿+通知(推荐) / B 逐一点采纳(当前) / C 自动建并直接发布。下一步还需做「广入库」从 VNDB 整批抓同人目录（分批+限流+同人过滤），与审核量解耦。
- 已实现采纳模型 A：抽出 work-service.createDraftGameFromWork（幂等，按 Work 融合字段建未发布 Game 草稿并关联）；request-inclusion 路由改为提交即自动建草稿+记 APPROVED 请求（游客可提，已含 gameId 返 409）；后台 /admin/inclusion-requests 改为「待发布草稿」列表（发布/删草稿）；nav 红点改 count 未发布且 galvelicaWork 存在的 Game（inclusionDrafts）。tsc 0 error、eslint 0 error。剩余=广入库（Stage G）才能真让 Galvelica 出现大量未收录作品→申请收录可见。
- **重要纠正（用户原话「你是不是忘了我们副站本来就是资料馆」）**：我前一轮把"回填"当 Galvelica 已建好、把"广收录整批同人目录"包装成可后推的 Stage G，是严重的表达错误。Galvelica=资料馆是 2026-07-25 原始设定（副站要收录整个同人 VN 生态、DB 远大于 Circleica），广收录整批目录是它的**本体/核心**，不是可选项。当前已落地的 A-F 只是地基+种子（仅含你 Circleica 目录的游戏），真正的资料馆缺的就是"从 VNDB 整批抓同人目录"。重新定位：广入库是 Galvelica 的核心功能，必须做，不是"以后扩展"。VNDB 客户端 sendRequest("vn",{filters,page,results}) 可分页枚举目录，validateDoujinWork 现成同人判定可用，技术上直接能做。
- **已落地广收录核心（用户选"全量同人"）**：① vndb.ts 新增 listVisualNovels({filters,fields,page,results,sort,reverse}) 分页枚举（复用 sendRequest+缓存，返回{results,more}）；② work-service 抽出 upsertWorkFromRaw(sourceKey,externalId,raw,opts)（不再二次拉取，供 getOrCreateWorkFromSource 与广收录共用），getOrCreate 改为先 fetch 再委托它；③ 新增 scripts/ingest-vndb.ts（npm run galvelica:ingest-vndb）：按同人标签(默认 6229，GALVELICA_DOJIN_TAG 可改；GALVELICA_INGEST_FILTER 可整段覆盖)分页抓，列表直出 rich fields → 每条包装 {results:[vn]} 直接归一化（命令数≈页数而非作品数），带限流(默认 6s/页)+断点续跑(.galvelica-ingest.json，GALVELICA_INGEST_RESET=1 清)+HARD_LIMIT 调试上限；首页空给明确告警。④ package.json 加脚本；.gitignore 加状态文件；架构文档 §11 将广收录从"可选 Stage G"纠正为 Galvelica 核心、执行清单升为第 3 步。tsc 0 error、src 改动 eslint 0 error（脚本 eslint 忽略）。
- **用户敏感存储占用**：问"全部存网站里会占很多内存吧"。澄清：① 不占 RAM——网站分页查询、ingest 是一次性批处理不常驻；② 占的是本机 Postgres 磁盘，全量同人目录（几万部）若留 raw 缓存整库几百 MB～近 1GB（大头 WorkSource.raw）；③ 全量同人目录可选，只跑 backfill 存储可忽略。
- **已落地省空间模式（回应存储担忧）**：work-service.upsertWorkFromRaw 融合后若 `GALVELICA_KEEP_RAW==="0"` 则 `workSource.updateMany raw=Prisma.JsonNull`（砍 70–80% 存储，仅留 source 行记 externalId 以便重拉）；ingest-vndb.ts 顶部默认 `GALVELICA_KEEP_RAW=0`（批量建库默认省空间）；fuseWork 加 `if (s.raw==null) continue` 防护避免紧凑模式下重融合命中 null raw 崩溃。架构文档 §11 加「存储说明」。tsc 0 error、eslint 0 error。用户要全量同人时跑 `npm run galvelica:ingest-vndb` 即自动省空间，想离线重融合则 `GALVELICA_KEEP_RAW=1 npm run galvelica:ingest-vndb`。
- **⚠️ 用户强烈要求：改动必须 deploy 安全、纯加法、不能搞乱他现有部署（Docker+pm2，docker-compose 有独立 migrate 服务跑 `prisma migrate deploy`）**。核对结论：① Galvelica 仅新增 Prisma 模型=多一个迁移文件，他现有的 `docker compose run --rm migrate` 步骤会自动应用，部署方法不变；② 即使部署后不生成迁移/不灌数据，galvelica.ts 的 archiveReady() 优雅回退到 Game 视图→绝不会崩现有站；③ 新增 env(BANGUMI_*/GALVELICA_*) 全可选、优雅降级，无需改 docker-compose；④ 新 admin 路由/页面纯加法。唯一需用户侧动作=本机跑一次 `prisma migrate dev --name galvelica_stage_a` 生成迁移文件(进 git)，之后部署全自动。此「deploy 安全/加法优先」是后续所有 Galvelica 改动的铁律。
- **多源扩展落地（用户要"更严+更广+跳过稀疏源"）**：① 新增 `sources/doujin-gate.ts` 严格同人闸门——默认 `GALVELICA_DOUJIN_ONLY=1` 只收带同人标签源(VNDB/Bangumi)，galge 广义源(月幕)默认跳过，设 0 放开。② 新增**月幕 YmGal 适配器**(`sources/ymgal.ts`，OAuth2 公共凭证 ymgal/luna0327，补中文译名/别名/封面/制作人员；Game 模型无 tags，标签仍由 VNDB/Bangumi 提供)+ `scripts/ingest-ymgal.ts`(按发售年翻页枚举+断点续跑+首年不可达优雅退出)+ npm 脚本 `galvelica:ingest-ymgal`。③ `YMGAL` 加入 `WorkSourceType` 枚举 + `SourceKey` + 融合优先级表(title/aliases/description/coverImage/releaseDate 兜底)。④ ErogameScape 因中国 IP 可能墙+API 不稳→暂未实现(待可达再加)；明确排除 nhentai/jandapress 等成人同人志(manga)。tsc 0 error、eslint 0 error（脚本忽略）。用户跑月幕需 `GALVELICA_DOUJIN_ONLY=0 npm run galvelica:ingest-ymgal`。
- **源 landscape 澄清（用户问 EGS/Cngal 是什么、新加源有无同人）**：① 实际只落地了**月幕 YmGal** 一个适配器；EGS(批评空间) 仅研究、因中国 IP 墙未实现。② EGS = ErogameScape = 批评空间（日语「エロゲー批評空間」），日本 eroge 评分/资料库，数据量堪比 VNDB，同人社团作品是主力，但**严格限制非日本 IP、中国基本连不上**→服务器不在日本就不接。③ **Cngal（CnGal 资料站 cngal.org）**=国内非营利 galgame 资料站，专收「国内制作组创作及中文化的中文 Galgame/AVG」=国产同人大本营，**有开放 Swagger API**（cngal.org/swagger），是中国可达且气质最贴的候选源，强烈建议接入。④ 中文向"中国可达"三角=月幕+Cngal+Bangumi；EGS 仅服务器在日本才值得接（稀疏源跳过逻辑会让它连不上自动放弃）。用户尚未拍板是否加 Cngal 适配器。

- **已落地 CnGal 适配器 + Steam 发现层（用户拍板"全球同人+搜源里可能没有的新同人"）**：用户提供 CnGal API 令牌（已写入 `.env` 的 `CNGL_API_TOKEN`，`.env.example` 加占位）。① 新增 `sources/cngal.ts`：`GET /api/entries/GetEntryView/{id}` 取详情、`POST /api/entries/GetEntryHomeList` 搜索、`GET /api/entries/GetPublishGamesByTime?year&month` 按月列游戏（匿名可访问，配 `CNGL_API_TOKEN` 则 Bearer 提额，缺失优雅降级）；`listCngalByMonth()` 供广收录。② 新增 `sources/steam.ts`（发现层，无需密钥）：`storesearch` 搜 + `appdetails` 取详情，`isVisualNovelApp()` 仅放行 genre 含 "Visual Novel"/"Dating Sim"；`searchSteamGames`/`fetchSteamAppDetails` 供发现脚本。③ `scripts/ingest-cngal.ts`（按年月列当月游戏→逐 id 拉详情建 Work{CNGL} 并融合，限流 300ms、断点续跑 `.galvelica-ingest-cngal.json`）；`scripts/ingest-discovery.ts`（多语种关键词搜 Steam→VN 校验→建 Work{STEAM} 候选，去重、断点续跑 `.galvelica-ingest-discovery.json`）。④ 接线：`CNGL` 加入 `WorkSourceType` 枚举 + `SourceKey` + 融合优先级表(title/aliases/description/coverImage/releaseDate 兜底)；`DOUJIN_CURATED` 扩为 [VNDB,BANGUMI,CNGL,STEAM]（CnGal/Steam 默认收录，符合"全球同人"）；registry 注册两适配器；package.json 加 `galvelica:ingest-cngal`/`galvelica:ingest-discovery`；.gitignore 加两个状态文件。⑤ 真实 API 已验证：GetEntryView/GetPublishGamesByTime 返回 200、Bearer 令牌可用、GetPublishGamesByTime 返回当月 Game 数组。tsc 0 error、eslint 0 error（脚本忽略）。⚠️ agent 内 `prisma generate` 因沙箱文件锁仅更新了 TS 类型（CNGL 已进 `index.d.ts`）、引擎二进制 rename 被 EPERM 拦，本机跑 `prisma migrate dev --name galvelica_cngal` 会一并修好；新增 `CNGL` 枚举需该迁移（或 `prisma migrate dev`）。用户本机执行清单现含第 5 步 ingest-cngal、第 6 步 ingest-discovery。

- **部署可复现性收口（用户是完美主义者、担心开源后别人部署缺手动步骤）**：把 Galvelica 资料馆填充焊进标准部署流程，做到「新机严格按文档即可完整跑起来、无需我们额外跑代码」。① 新增 `ingest-entrypoint.sh`（含 DB 连通等待 + 幂等/断点续跑的填充流程：backfill→ingest-vndb→ingest-cngal→ingest-discovery，月幕仅 `GALVELICA_DOUJIN_ONLY=0` 时跑；单源失败不中止、结尾汇总）。② Dockerfile：builder 阶段 COPY+chmod `ingest-entrypoint.sh`（复用 builder 含全量 src+tsx，不污染 runner 镜像）；runner 阶段同步 COPY 该脚本。③ docker-compose.yml 新增 `ingest` 服务：`build.target: builder`、`entrypoint: /ingest-entrypoint.sh`、复用 db 健康检查、`profiles: [ingest]`（默认 `docker compose up -d` 不会启动它，与 migrate 同款）、传 DATABASE_URL/CNGL_API_TOKEN/BANGUMI_ACCESS_TOKEN/GALVELICA_DOUJIN_ONLY/GALVELICA_KEEP_RAW。④ DEPLOYMENT.md：方式一（Docker）与方式二（pm2 手动）都加了「填充 Galvelica 资料馆（可选但推荐）」步骤，并注明数据持久化于 Postgres、重部署不丢、未填充时站点正常（空状态兜底）。⑤ `.env.example` 补 `GALVELICA_DOUJIN_ONLY`/`GALVELICA_KEEP_RAW` 说明。校验：`sh -n` 通过、compose YAML 与 `migrate` 同构合法。现在别人部署只需 `docker compose up -d`(+可选 `docker compose run --rm ingest`)，无需我们介入。

- **首页骨架屏修复（用户问「为啥只有刷新才闪、客户端导航不闪」）**：根因=Next.js 流式 SSR + `<Link>` prefetch 的预期行为——首页仅游戏网格有 `<Suspense fallback={<GameGridSkeleton/>}>`（page.tsx:240），无 `src/app/loading.tsx`；公告区 `{announcements.length>0 && <AnnounceSwiper/>}`（page.tsx:216）本身无骨架。硬刷新服务端重渲染→Suspense 回退先流出→见骨架；客户端软导航时 RSC 负载已 prefetch 缓存、内容就绪→回退不渲染。用户选「加 route-level loading.tsx」。新增 `src/app/loading.tsx`：完整镜像首页布局（品牌卡占位 h-[310px] + 公告占位 h-[200/220/310px] + 移动端按钮占位 + 网格 12×GameCardSkeleton），用 `skeleton-shimmer`（globals.css:764 自带渐变底+动画）。效果：路由级 Suspense，刷新与客户端导航都一致显示骨架，并补了公告区占位（原无）。eslint 0 error、tsc 0 error。纯加法，不动 page.tsx。
- **games 列表页骨架屏（用户 ok 后补全同款）**：`src/app/games/page.tsx` 也有 `<Suspense fallback={<GridSkeleton/>}>`（line 151）但无路由级 `loading.tsx`，同款「仅刷新闪」。新增 `src/app/games/loading.tsx`：镜像页面结构（头部标题占位 h-7/h-4 + 工具栏占位条 + 默认 grid 视图 12×GameCardSkeleton），复用组件内 `GameCardSkeleton`。eslint 0 error、tsc 0 error。纯加法。
- **全站路由级骨架屏收口（用户「完美主义、别再问」）**：把用户可见的内容/浏览页全部补上 route-level `loading.tsx`，统一「刷新与客户端导航都一致显示骨架」。本轮新增：`tags/loading.tsx`（镜像标签云 h-48 + 分类网格 + 索引 h-96，用 Skeleton）、`discover/loading.tsx`（页头 + 6 区块各标题条+5×GameCardSkeleton）、`ranking/loading.tsx`（页头 + 维度 nav + TOP3 网格 + 8×GameListRowSkeleton）、`curated-collections/loading.tsx`（宽卡+网格）、`curated-collections/[id]/loading.tsx`（hero aspect 21/9 + 信息 + 8×游廊行）。**刻意跳过**：① `credits`（纯客户端 `CreditsClient` 自管 loading，路由级无意义）；② `profile/[id]`（只是 redirect 到 `/user/[serialId]`，后者已有 loading.tsx）；③ 登录/注册/找回密码/验证邮箱/setup/about/contact/rules 等表单或静态页（加骨架每导航都闪、反人类）。`galvelica/*` 已由父级 `galvelica/loading.tsx` 统一覆盖。`search/loading.tsx` 早已有。eslint 0 error、tsc 0 error。纯加法。

- **融合优先级表重排（用户提醒「不止这几个源、权威优先高质量优先」）**：QA 报告只列了 VNDB/CnGal/Steam，用户指我漏了 Bangumi/月幕等且融合优先级没体现权威/质量。核查 `fusion.ts` FUSION_TABLE 发现缺陷：`STEAM` 只出现在 `steamAppId`，但 `coverImage`/`releaseDate`/`description` 三数组完全没它——而 `steam.ts` normalize 明明吐出 `header_image` 封面和发售日，等于 Steam 的高质量字段在融合时被无视。修复：① 把 STEAM 补进 `coverImage`(紧随 VNDB，高分辨率商店图)、`releaseDate`(排中文源之后作兜底，因其格式偶不规整)、`description`(短描述兜底)；② 整体按「每字段谁最权威/质量最高谁排前」重排并写清各源权威面注释：VNDB=canonical 元数据最权威、BANGUMI=中文译名/别名/简介质量最高、YMGAL=补中文译名/封面/人员(无 tags/无稳定社团名)、CNGL=国产同人封面与制作组完整(无 tags)、STEAM=高质量封面+可靠发售日、DLSITE=官方购买链接(预留)、MANUAL=人工兜底(外部权威存在时让位)；`studioName` 补 CNGL(有 productionGroups)，月幕/Steam 无稳定社团名不纳入。tsc 0 error、eslint 0 error。注：此表仅对「同一 Work 含多源」时生效；当前各 Work 仅单源(跨源融合未发生的 76 重复组问题属另一期，需跨源匹配器)。
