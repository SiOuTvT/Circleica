# 2026-07-22

## Production Readiness Review（上线验收，2026-07-22）

从架构一致性收敛切换为 Go-Live Review。5 路并行探查（部署/Env、Prisma/迁移、Redis/后台、安全/存储、可观测性/SEO/灾备）+ 亲自核对修复。

### 自动修复的 4 项上线阻断（tsc 验证零错误）
1. `docker-compose.yml`：移除 `backup` 服务 `profiles: ["backup"]` → `docker compose up` 现在会启动每日 `pg_dump` 备份（原配置下备份容器不启动 = 无备份运行）。
2. `Dockerfile` + `docker-compose.yml`：为 `NEXT_PUBLIC_TURNSTILE_SITE_KEY` 增加构建期 `ARG/ENV` 与 build arg（此前仅运行时注入，客户端 bundle 构建期为 undefined → 生产验证码静默失效）。
3. `src/lib/auth.ts`：生产环境（`NODE_ENV==="production"`）强制 Cookie `secure: true`，不再仅依赖 `NEXTAUTH_URL` 协议。
4. `src/repositories/game.ts`：`$queryRawUnsafe` → 参数化 `Prisma.sql`（已把 `import type { Prisma }` 改为值导入）。

### 仍为 Blocking 的部署环境变量（运维必须在 Coolify 设置）
- `NEXTAUTH_SECRET`：未设置时入口脚本在容器内 `/app/.secret`（非持久卷）自动生成 → 每次重建容器全体会话失效。
- `NEXTAUTH_URL`：未设置回退 localhost:3000 → 邮件链接/OG/secure cookie 回退错误。

### 关键核查结论（无阻断）
- 数据库/迁移：`migrate deploy` 已在 `docker-entrypoint.sh` 自动执行；migrations 与 schema 一致；无 SQL 注入；Prisma 单例正确；密码 bcrypt 哈希。
- 请求层/架构一致性（前序）：已收敛干净，本轮不重复。
- 安全：CSP/Headers/X-Frame-Options/HSTS 在 middleware 设置；限流覆盖 auth 端点；Cookie httpOnly/sameSite=lax；上传通道有类型/大小/鉴权校验。
- 稳定性：Prisma/R2/Redis 均单例，客户端定时器均有 cleanup，无内存/连接泄漏；无裸 `new PrismaClient` 每请求。
- 存储：未配 R2 时回退本地 `public/uploads`（经 `uploads_data` 卷持久化），上传不会 500 → R2 为 Recommended（多副本时必须）。

### 交付物
- `D:\fangame\production-readiness-review.md`（完整报告 + Checklist + 结论 **Go（条件：设置上述 2 个 env）**）

### 后续 Recommended（非阻塞）
Sentry DSN 设置；邮件改为后台队列+重试；缓存失效补齐；限流全局兜底；SVG 上传排除；备份异地化+R2；POSTGRES_PASSWORD 改 secret；Dockerfile 运行期去 devDeps；低频索引；request-id/Sentry.setUser 归因。

## Final Launch Readiness Review（最终上线演练，2026-07-22 晚）
身份 Principal/SRE，模拟真实上线。不重复代码/架构/风格问题。

运维面实测结论：
- 健康检查 `/api/health` 健壮：DB 失败→503(unhealthy)，Redis 缺失→`disabled`(不影响 healthy)，Redis 错误→`degraded`(不致命)。`start_period 30s`+`depends_on db healthy` 防启动期重启循环。
- 迁移：`migrate-entrypoint.sh` 与 `docker-entrypoint.sh` 均用 `prisma migrate deploy`(前向、幂等)；migrations 目录 6 个 + lock(provider=postgresql)，与 schema 一致。失败 exit 1（fail-fast，不以坏 schema 启动）。
- `DEPLOYMENT.md` 已含备份/恢复命令：`docker exec fangame-db pg_dump ...` / `cat backup.sql | docker exec -i fangame-db psql ...`。
- 备份容器 profile 阻断已在上一轮修复，`docker compose up` 会启动 fangame-backup（每日 03:00 pg_dump，保留 7 天，backup_data 卷）。
- 无新增代码级阻断；回滚关键：Prisma 迁移前向-only（无 down），DB 回滚=备份恢复；应用回滚靠 Coolify 上一部署/镜像 tag。

最终结论：**Go（条件）**——上线前置仅在 Coolify 设 `NEXTAUTH_SECRET` + `NEXTAUTH_URL`；建议设真实 VERSION tag、SENTRY_DSN、多副本时配 R2/Redis；上线 24h 内做一次备份恢复演练。

## Post-Launch Guardian 模式（22:17 切换）

用户宣布项目已正式上线（Go Live），角色切换为 **Principal Engineer + SRE + Tech Lead**，进入长期稳定保障模式。

### 运维铁律（已写入 MEMORY.md）
- 不为追求完美改稳定代码；不主动大规模重构；不制造新债。
- 仅在发现**真实**风险/缺陷/性能瓶颈/线上问题时才改。
- 优先级：P0 故障·数据安全·权限·漏洞·丢失·不可用 → P1 真实用户反馈·崩溃·性能·泄漏·DB/缓存异常 → P2 体验(不影响功能) → P3 新功能 → P4 优化/重构(仅收益明显时)。
- 任何修改流程：定位→影响分析→最小方案→修复→自动验证→确认无回归→变更报告。
- 不再重复审查已确认无问题的模块；不重新生成历史问题；不为"还能更好"改稳定代码。
- 目标：长期稳定·可维护·可持续迭代。

## 深度质量收敛（22:22 起，连续三轮扫描模式）

用户要求退出维护模式、回到深度质量收敛：扫描→修复→验证→再扫描，直到连续三轮无新问题。不重复历史问题、不重出历史报告。

### Round 1（输入校验/鉴权/错误韧性）— 已修
- **[P1 已修]** `src/lib/email-providers.ts`：resend/brevo `fetch` 无 `AbortSignal` → 慢提供商挂起核心鉴权流程（注册/找回密码/改邮箱）。加 `signal: AbortSignal.timeout(10_000)`，catch 已吞 AbortError→优雅失败。tsc 零错误。
- **[P2 已修]** `src/app/api/sentry/tunnel/route.ts`：`JSON.parse(piece)`/`new URL(header.dsn)` 无防护 → 畸形 envelope/缺 dsn 返回 500（应为 400）；转发 `fetch` 无超时→挂起。已包裹 try/catch 返回 400 + 校验 dsn 字符串 + 转发加 `AbortSignal.timeout(10_000)`。tsc 零错误。
- **Round 1 已确认 clean（不再复查）**：数据层 IDOR/越权（所有权校验扎实）、SQL 注入（全参数化）、鉴权闸门（~110 handler 全有 guard）、非 2xx 静默吞（服务端无 apiFetchSafe 用法）、命令注入。
- **Round 1 遗留 P2（记录，未强修）**：admin 路由未用 `lib/validations.ts` 既有 zod schema；`gameResource.findMany` 公开端点无 `take` 上限；game report `reason` 无长度校验；VNDB 批量导入顺序 await 无上限；follow toggle 读后写无事务（唯一约束防损坏，仅双点击返回 409/404）。

### Round 2（性能/缓存/限流/SEO/依赖）— 已修
- **[P1 已修]** `src/lib/rate-limit.ts` `getClientIP`：原取 `X-Forwarded-For` **首段**（客户端可伪造）→ 所有 IP 限流可绕过（暴力破解/批量注册失效）。改为优先 `CF-Connecting-IP`/`X-Real-IP`，否则取 **末段**（可信代理追加的真实客户端 IP）。`trustProxy:true` 下正确。
- **[P1 已修]** `src/lib/rate-limit.ts` `checkRateLimit`：`Retry-After` 误把绝对 epoch 秒当 delta → 客户端退避算错。改为 `Math.max(0, (resetTime-Date.now())/1000)`。
- **[P2 已修]** `src/app/api/search/route.ts` + `suggestions/route.ts`：公开搜索端点无限流（DoS/抓取）。接入既有未使用的 `rateLimits.search`（30/min）。
- **[P2 已修]** `src/app/api/auth/forgot-password/route.ts`：原仅按 IP 限流 → 多 IP 可向同一邮箱轰炸重置邮件。改为按**目标邮箱** `getRateLimit(\`pwreset:${email}\`)` 限流。
- **[P2 已修]** `src/app/user/[id]/page.tsx` `resolveUser`：全表读取（含 password 哈希）且 generateMetadata/页面各调一次无去重。加 `select` 仅取所需字段 + `reactCache` 请求内去重。
- **[P1 已修]** 依赖：`sharp ^0.34.5` → `^0.35.3`（libvips CVE，用户上传头像处理攻击面）；`npm install` 确认顶层 sharp=0.35.3。另将仅构建期使用的 `shadcn` 从 dependencies 移到 devDependencies（缩小生产审计面）。tsc 零错误。
- **Round 2 确认 clean（不再复查）**：SEO/metadata 完整（sitemap/robots/OG/canonical 齐全、无公共页误 noindex）；Redis 单例+优雅降级；缓存按用户隔离无跨用户泄漏；N+1 热点无；串行 await 已并行化；无硬编码密钥。
- **Round 2 遗留 P2（记录）**：`sharp` 仍被 `next@16.2.7` 作为传递依赖锁 0.34.5（仅影响 `/_next/image` 优化端点，非直接 import；可考虑 overrides 强制 0.35.x）；首页 GameGrid 因 searchParams 变 force-dynamic 每请求打 DB（应 `unstable_cache`）；`games/[id]` 公开 API 返回整 creator 行+无 `take` 资源；sitemap 未含论坛/用户/角色页；checkin 缓存键裸 `checkin:config:*` 逃逸 `fangame:` 命名空间；Redis 故障 fail-open（设计取舍）。

### Round 3（数据完整性）— 确认 clean + 记录
- 级联删除：所有父→子关系均有 `onDelete: Cascade/SetNull`，无孤儿（已逐关系核对 schema）。软删：无此模式。字段长度：全部 `String`→Postgres `text`，无 varchar 溢出。
- 唯一约束缺口（P2，需迁移/产品决策，未强改）：Notification 无去重 `@@unique`（重试会插重复行）；`GameResourceEntry` 无 `@@unique([resourceId,url])`（同 URL 重复提交）；`GameResource` 无 `@@unique([userId,gameId])`（双提交，可能属"多镜像"可不修）。其余 one-per 不变量均有 DB 唯一约束。

### Round 4（后台任务/SDK/CSP/限流拓扑）— 已修 P1
- **[P1 已修]** `src/lib/redis.ts`：`cache = createCache()` 在**模块加载时**固化后端，而 `service-config` 的 Redis 配置是异步加载（首访 `_redis` 为 null）→ `cache` 永久是 `MemoryCache`。即使后来 `isRedisAvailable()` 返回 true，`rate-limit` 仍走内存计数器 → **分布式限流从未真正连 Redis，多副本下认证限流被绕过**（此前修的 XFF 防伪造/按邮箱限流都建立在此失效假设上）。改为惰性委托代理 `cache`：每次调用按 `getRedisConfig()` 实时值选 Redis 或内存后端，配置就绪后自动切 Redis。`cached()` 通用缓存也随之真正用上 Redis。tsc 零错误。
- **[P2 已修]** `.env.example`：补 `BREVO_API_KEY`、`EMAIL_PROVIDER_ORDER`（生产邮件功能所需但文档缺失）。
- **Round 4 确认 clean/记录**：CSP 强（`script-src 'self' 'nonce' 'strict-dynamic'`，无 unsafe-inline；`connect-src` 白名单非 `*`；`frame-ancestors 'none'`/`object-src 'none'`/`base-uri 'self'`）；HSTS(max-age=1y+includeSubDomains)；X-Frame DENY；NextAuth v5（jwt+缺 secret 生产 exit(1)+role 默认 USER+trustHost）；Uploadthing 仅 image/audio 无任意类型（SVG 内联 XSS 为 P2 建议收窄 contentType）；Prisma 连接池已设 connection_limit/pool_timeout（多副本建议下调）；VNDB 导入事务缺失+`vndbId` 无唯一（P2，admin 域，建议迁移+事务）；限流各认证 mutation 全覆盖无缺失。
