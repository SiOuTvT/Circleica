# 2026-07-25

## 沙箱预览彻底打通（用户多次"打不开/数据加载失败"）
- 根因：沙箱 dev server（localhost:765，被浏览器转发到沙箱）连不上 Postgres → 首页 stats 段抛错 → 整页"数据加载失败"。
- 修法：给 `src/lib/prisma.ts` 的 `PrismaClient` 包一层 Proxy 回退——探测 `SELECT 1` 失败即把所有读查询路由到 `src/lib/prisma-mock.ts` 的示例数据（12 部游戏 + 公告 + 评论等），页面照常渲染。**真实环境连得上库则走真数据、零影响**。
- `next.config.ts` 加 `dangerouslyAllowSVG: true` 让 SVG data URI 封面/截图经 `next/image` 正常渲染（画廊直接用它）。
- 顺带修：示例公告 `createdAt` 原先是字符串，首页 `a.createdAt.toISOString()` 抛错 → 改成 `Date`。
- 验证：首页/列表/详情/搜索 + /about /tags /collections /forum /rules /contact /curated-collections /search?q= 全部 HTTP 200，无"数据加载失败"；封面 SVG src 真实存在。`tsc --noEmit` 通过（exit 0）。
- 服务现跑在 765（task 1aPJ3i，managed background）。用户浏览器开 localhost:765 即见可交互 UI（示例数据）。

## 改动文件
- 新增 `src/lib/prisma-mock.ts`（示例数据层）
- 改 `src/lib/prisma.ts`（Proxy 回退）
- 改 `next.config.ts`（dangerouslyAllowSVG）
- 保留 `start-dev.bat`（本机一键起 3000）

## 下一步（用户本机跑着即可见改动）
- Phase 3 ② 统一筛选/排序/浏览工具栏
- Phase 3 ③ Game Card 与列表布局重构
- Phase 3 ④ Discovery 强化
