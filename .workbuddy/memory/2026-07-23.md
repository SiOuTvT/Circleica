# 2026-07-23 工作日志

## 全站 UI 资源完整性巡检（Production Asset Audit）

背景：生产环境后台「空状态」页面 Lucide 图标渲染成原始字符串（Map / Lock / Image / BookOpen / MessageCircle / Inbox / CheckCircle / Package …）。

### 根因（已实证）
`src/lib/emoji-icons.ts` 旧实现用 `const MAP = { Heart, CheckCircle, ... }` 对象字面量 + `MAP[name]` 动态查表。
生产构建下（Turbopack/webpack usedExports + lucide-react `sideEffects:false`），这些"仅被动态 key 读取"的属性被 Tree-Shaking/DCE 整组删除，
导致 `getIconForEmoji` 返回 null → `EmotionalIcon` 回退到 `<span>{name}</span>`（渲染字符串）。
实证：旧产物 `.next/static/chunks` 中 grep `BookmarkPlus`（仅被 emoji-icons.ts 使用的图标）未命中 → 证明注册属性被 DCE 删除。Dev 不压缩故正常。

### 修复（同类型一起修）
1. `src/lib/emoji-icons.ts`：改 `switch(name){ case "X": return X }` 静态返回，每个组件在可达路径被静态引用，无法被 DCE；导出静态 `EMOJI_OPTIONS`（`as const`）供 admin 下拉；新增 `Cloud`。
2. `src/components/tag-position-checkbox.tsx`：删除 `ICON_MAP` + lucide 直接 import，改用 `getIconForEmoji(pos.icon)`（同一动态按名查表模式的同类隐患）。
3. `src/app/layout.tsx`：删除无用的 `notoSans = { variable: "" }` 占位；同时修掉删除后残留的两处 `${notoSans.variable}` 引用（避免引入 TS 编译错误）。
4. `public/icon-192.png` + `public/icon-512.png`：新增 PWA 图标（用 `next/og` 的 ImageResponse 生成，与 `icon.tsx` 同款 🎮 设计）；`manifest.ts` 原已引用这两个路径，404 修复。生成脚本留 `scripts/gen-pwa-icons.cjs`。

### 维度结论
- 一、Lucide 图标：1 个真实缺陷（DCE）已修，同类隐患 tag-position 已修。
- 二、Emoji：0 缺陷（全部 unicode 文本渲染，无外部 CDN emoji 图，统一经 EmotionalIcon）。
- 三、字体：0 缺陷（系统字体栈 PingFang/YaHei/Source Han，无 next/font/google 或 Google CDN link；删除无用 notoSans）。
- 四、SVG 与 Icon：0 新增缺陷（PWA 图标 404 属静态资源维度，已修）。
- 五、静态资源：1 个真实缺陷（manifest 引用缺失的 icon-192/512.png）已修。
- 六、部署差异：Dockerfile 正确 copy public/ 进 standalone 镜像；DCE 是唯一 dev≠prod 差异，已修。

### 验证（最终）
- `tsc --noEmit`：exit 0（修 layout.tsx 回归后）。
- 生产 `next build`：本沙箱环境挂起（两次均 15 分钟零输出，疑似构建期预渲染等待 DB / 沙箱 I/O 限制），未能跑完。属环境问题，与图标改动无关。
- 实证替代方案（faithful）：用 rollup 对**修复后的真实 `emoji-icons.ts`**（改写 lucide 为绝对路径导入）+ 真实 `lucide-react` 做 tree-shaking 打包（非压缩），执行产物确认 `getIconForEmoji` 对全部 50 个 EMOJI_OPTIONS 返回 **0 个 null**（全部 OK）→ 证明 switch 模式在生产 tree-shaking 下图标不会被摇掉。
- bug 侧证据：运行时现象为铁证（生产渲染原始图标名字符串、开发正常渲染 SVG）；旧产物 grep 图标名不可靠（压缩后函数名被改写，连直接 import 的 Loader2 也搜不到），故不依赖 grep 作为铁证。

### 变更文件（待提交）
- `M src/app/layout.tsx`：修 notoSans 回归（删两处 `${notoSans.variable}`）。
- `?? public/icon-192.png` / `?? public/icon-512.png`：新增 PWA 图标（next/og 生成）。
- `?? scripts/gen-pwa-icons.cjs`：PWA 图标生成器（可重复生成）。
- 已提交 `4b48f83`：`emoji-icons.ts` switch 化 + `tag-position-checkbox.tsx` 同类型修复。

## 结论
- 全站 UI 资源完整性巡检完成：修复 2 个真实缺陷（Lucide DCE、PWA 图标 404），同类隐患（tag-position 动态查表、无用 notoSans）一并清理。
- 6 段式审计报告已交付用户。dev==prod 的核心差异（DCE）已通过 switch 模式根除；剩余 `next build` 实测建议用户在自身 CI/部署环境执行一次目视确认。

## 全局品牌重构：Fangame / 同人游戏站 → Circleica

用户确定正式品牌名 **Circleica**，指令全局替换旧临时名。新标准：SEO Title=`Circleica - 专注同人视觉小说资源收录`；页脚=`Circleica - 极客同人社区 | 完全免费开放的视觉小说档案库`。

### 已替换（展示层/配置/文档）
- 源码展示层（16 文件 23 处）：layout.tsx metadata(title.default=`Circleica - 专注同人视觉小说资源收录`、template=`%s · Circleica`)、site-settings/service-config fallback、邮件 fromName/测试主题、validations/email-providers 默认值、announcements-manager/setup-wizard/layout-wrapper 文本、card-generate-btn 水印、Footer 文案、4 处第三方 API User-Agent(FangameNext/Admin→Circleica/Admin)。
- 旧站名"同人游戏站"（26 文件 45 处）：各页 metadata、`about` 页、`opengraph-image`、setup-wizard、announce-swiper、e2e 断言等 → Circleica。
- manifest.ts：name/short_name/description 改 Circleica 品牌与定位。
- 工程文件：package.json name→circleica；package-lock.json 顶层+packages[""] name 同步。
- 文档 README/DEPLOYMENT/AGENTS/docs/*.md 等：Fangame→Circleica。
- DB：`scripts/update-brand.sql` 已执行（改用幂等 UPSERT；原 UPDATE 因该库 SiteSetting 表从无 site_name/site_description 行会 0 匹配）。

### 保留（技术/基础设施标识，改了会制造回归，不改）
- cookie 名 `fangame-session-token`（middleware/auth）：改→登出所有已登录用户。
- redis key 前缀 `fangame:`（redis.ts）：改→全站缓存冷启动。
- 硬编码 secret `local-dev-secret-key-for-fangame`/`fangame2024`（security-check.ts）：安全密钥，与品牌无关。
- DB 实例名/容器名/volume/PM2 名/cwd/SSL/NGINX 配置名（docker-compose*/DEPLOYMENT/ecosystem.config.js）：基础设施标识，改名需重建 DB+改连接串。
- 目录名 D:\fangame：改→破坏所有路径引用。
- 调试脚本 find-*.py / e2e_full_test.py、预览 HTML：非产品代码。

### 验证
- `tsc --noEmit`：exit 0。
- "同人游戏站" 全仓清零；源码 fangame 残留仅上述技术标识。

### 数据库执行结果（已做）
- 通过 Prisma 客户端对本地 Postgres（`DATABASE_URL` 来自 `.env`，库 `fangame`）执行 UPSERT。
- 发现：`SiteSetting` 表共 9 行，但**从无 `site_name`/`site_description` 行** → 站点名此前一直由代码 fallback 驱动。
- 结果：已 UPSERT 写入两行，DB 现权威持有 `site_name=Circleica`、`site_description=Circleica - 极客同人社区 | 完全免费开放的视觉小说档案库`，验证 OK。
- `update-brand.sql` 已改为 `INSERT ... ON CONFLICT("key") DO UPDATE` 幂等形式，适配"行不存在则插入"的场景。
- 一次性临时脚本 `scripts/list-settings.mjs`/`run-brand-update.mjs` 已删；保留 `gen-pwa-icons.cjs` 与 `update-brand.sql`。

### 待用户侧
- 提交由用户自理（用户明确"提交不用你管"）；本会话资产巡检 + 品牌重构改动均未 commit。
- 沙箱跑不了完整 `next build`（挂起），建议用户在部署/CI 环境 build 一次目视确认（图标 SVG 渲染 + 品牌名显示）。
- 可选：若坚持改 cookie 名/DB 实例名，需单独评估（登出用户/迁移）。

## 品牌文档补漏（用户追评：部署指南步骤一仍有 fangame）

用户指出 DEPLOYMENT.md 步骤一 `git clone ...fangame.git /opt/fangame` 未改。此前我把小写 `fangame` 一律当"技术/基础设施标识"留着，用户要求文档也统一。
- 已补改（文档侧，安全）：`DEPLOYMENT.md` 32 处 `fangame→circleica`（含 clone URL、/opt 目录、volume、容器、pm2、nginx 证书名、示例 DB URL）；`README.md` 1 处（`cd fangame`）；`docs/GETTING_STARTED.md` 4 处（clone 目录 + 示例连接串）。
- **保留**：`code-review-report.md`/`production-readiness-review.md` 中的 `fangame` 是审计报告对当前源码真实状态的引用（docker-compose 弱口令、`fangame:*` redis 前缀），改了会与真实代码对不上。
- **保留（活基础设施定义，盲改=需迁移，危险）**：`docker-compose*.yml`、`ecosystem.config.js`、`docker-entrypoint.sh`、`scripts/setup-ssl.sh` 的容器名/volume/pm2 名/nginx 证书名；`package.json` 的 `psql -d fangame` 库名。
- **保留（源码技术标识，改了会登出用户/缓存冷启动）**：`middleware.ts`/`auth.ts` cookie 名 `fangame-session-token`；`redis.ts` key 前缀 `fangame:`；`security-check.ts` 硬编码 secret。
- **保留（调试脚本非产品）**：`find-*.py`、`e2e_full_test.py`、`check-body.py`、`test-user-page.py` 等。

### 关键后果（已告知用户）
- DEPLOYMENT.md 现描述 `circleica` 命名的部署，但**实际线上基础设施仍叫 fangame**（容器/库/volume/pm2/nginx 均定义在未改的 compose/ecosystem 文件里）。故文档的操作命令（`docker exec circleica-db`、`pm2 restart circleica`、`docker volume ls|grep circleica`、nginx `circleica.conf`）在**当前 fangame 命名的服务器上不匹配**。
- GitHub clone URL 改为 `SiOuTvT/circleica.git`，仅当 GitHub 仓库真改名为 circleica 才有效；否则 clone 失败。
- 若要做到文档与线上完全一致，需把 compose/ecosystem 等也改名为 circleica 并做迁移（dump DB→新建 circleica 库/容器/volume→更新 .env DATABASE_URL→重启），属独立高风险运维任务，需用户单独拍板。

## 全站改名执行（用户最终授权：全站都改，含活基础设施与源码技术标识）

用户明确"仓库名早改了，直接把全站都改"。执行了完整改名 fangame→circleica：

### 已改名（代码/配置/脚本层）
- 基础设施：`docker-compose.yml`(容器名 fangame-db/app/backup→circleica-*、POSTGRES_USER/PASSWORD/DB fangame→circleica、网络 fangame→circleica、DATABASE_URL 默认、备份 cron)、`docker-compose.dev.yml`(同)、`docker-entrypoint.sh`(启动横幅 Fangame→Circleica)、`ecosystem.config.js`(name/cwd fangame→circleica)、`scripts/setup-ssl.sh`(fangame.conf/crt/key→circleica.*)。
- 源码技术标识：`src/middleware.ts`+`src/lib/auth.ts` cookie 名 fangame-session-token→circleica-session-token；`src/lib/redis.ts` key 前缀 fangame:→circleica:(含注释/SCAN 模式)；`src/lib/security-check.ts` 硬编码 secret local-dev-secret-key-for-fangame/fangame2024→circleica 变体。
- 脚本/本地：`package.json` psql -d fangame→circleica；`scripts/export-theme.sql` 用法注释；`scripts/test-new-build.js` pm2 restart fangame→circleica；`.env` DATABASE_URL→circleica 库、NEXTAUTH_SECRET→circleica 变体、注释。
- 测试断言：`e2e_full_test.py:27` "Fangame" in title → "Circleica"（改名后 title 为 Circleica，否则误报 FAIL）。

### 数据库重命名（关键坑：Postgres Docker 中 POSTGRES_USER/DB 仅首次初始化生效）
- 生成 `scripts/migrate-db-rename.md` 生产迁移 runbook（ALTER DATABASE/USER RENAME + 终止占用会话 + 超管改名技巧 + 回滚）。
- **本地库**：已成功 `ALTER DATABASE fangame RENAME TO circleica`（数据保留，验证 site_name=Circleica 仍在）。但本地 `fangame` 用户**非超管且 postgres 超管口令未知**，无法改名；故本地 .env 保留 fangame 用户、指向 circleica 库（`postgresql://fangame:fangame2024@127.0.0.1:5432/circleica`），本地连接验证 OK。
- **生产**：用户服务器有 postgres 超管权限，按 runbook 可完成 user+db 全量改名（含登出用户/缓存冷启动等预期影响）。

### 验证
- `tsc --noEmit`：exit 0。
- 本地 Prisma 连 circleica 库 OK；SiteSetting 品牌值仍在。

### 全仓剩余 fangame（均有意保留，非遗漏）
- 用户追加指令"仓库名早改了，全改成 circleica" → 第二轮已把调试脚本/审计文档/dev_log/preview HTML 里的 fangame 全部改为 circleica（本轮 19 文件 59 处：Fangame→Circleica、fangame→circleica）。
- **仅剩两处刻意保留旧名**：
  1. `team-okr-radar.html`：描述另一产品（Electron 节奏游戏 "fangame" + ScriptWeaver），与本站品牌无关 → 不改。
  2. `scripts/migrate-db-rename.md`：DB 改名迁移 runbook，命令须写 FROM 旧名 fangame → 不改（否则 ALTER 指令失效）。
- **两项技术现实（非"漏改"，是改不了/不该改）**：
  - 调试脚本 `find-*.py` 等的 `d:/fangame/` 路径已改 `d:/circleica/`，但**磁盘目录仍是 `D:\fangame`**（无法在中途重命名自身工作目录）；这些一次性诊断脚本若运行需先把文件夹改名或还原路径字面量。
  - 本地 `.env` 数据库**用户名**仍是 `fangame`（`postgresql://fangame:fangame2024@127.0.0.1:5432/circleica`）：本地 postgres `fangame` 用户非超管且超管口令未知，无法改名；库名已成功改为 circleica。改用户会令本地开发连不上库。生产侧 docker-compose 已用 circleica 用户，按 runbook 在服务器执行即可。

### 待用户侧
- 提交由用户自理（"提交不用你管"）。
- 生产部署须按 `scripts/migrate-db-rename.md` 执行 DB 全量改名（否则应用连不上 circleica 库崩溃）；cookie 改名会登出所有用户、redis 前缀改名触发缓存冷启动，均属预期。
- 沙箱跑不了 `next build`，建议用户在部署/CI 环境 build 一次目视确认。
