# 2026-07-21

## 全项目代码审查（Production-Ready Review）
- 对 Fangame（Next.js 16 + Prisma 社区平台，~47k 行）做了完整审查，产出报告 `D:\fangame\code-review-report.md`。
- 方法：精读 lib/services/关键 API/上传链路 + 2 个并行广度审计 Agent（后端 admin+repo、前端+基础设施）+ 高危项回源核验。
- 最严重结论（上线前必修）：
  - H1 权限提升：`PUT /api/admin/users/[id]` 仅 `requireAdminRole()`，ADMIN 可提升 USER→ADMIN（应为 SUPER_ADMIN）。
  - H2 安全头：代理后 HSTS 永不下发（middleware 仅看 `req.nextUrl.protocol`，缺 `trustProxy`）。
  - M1 系统性：`withHandler` 不映射 Prisma 错误（P2002→500），连带放大多处竞态 500。
  - M4 数据一致性：后台删收藏/删用户不回退 `favoriteCount`。
  - M5 功能：签到时区边界不一致（上海日期按本地时区解析，streak 用 UTC 比较）。
- 亮点（非问题）：富文本 DOMPurify 净化稳健、Setup 用 Serializable 事务防重初始化、无散落 console.log/TODO、CSP 较强。
- 用户要求"全面、不遗漏、第二轮复查"——已做交叉核验与补充扫描（含管理员评论删除缺口、速率限制覆盖不足、孤儿上传文件等）。
- 注意：本轮为只读审查，未修改任何源码；报告中所列问题均待团队排期修复。

## Round 3+ 深度复查（Principal/Staff 级反复深挖）
- 在首轮+第二轮报告基础上做了第三轮定向复扫（全量 grep 同类模式 + 回源精读），新增 6 条确认问题（M15–M20），并修正 1 条既有结论。
- 新增（已写入 `code-review-report.md` 六之二节）：
  - M15：~41 个 API 路由直接 `req.json()` 无 try/catch → 畸形/空 body 返回 500 而非 400（系统性健壮性）；`parseBody` 是死代码。
  - M16：NextAuth `authorize` 无速率限制 → 登录可暴力破解；v5 第二参即 request，可接 `checkRateLimit` 按 IP 限流。
  - M17：管理员删帖/删评经 API 失效——`deletePost/deleteComment` 路由未传 `isAdmin`（帖子与评论同病），前端显示删除按钮但 403。
  - M18：成就 streak 计算（`lib/achievements.ts:32/39/45/60`）复用与签到相同的"上海时区/UTC 混用"缺陷，随 M5 同批修。
  - M19：用户自填 URL（avatar/banner/forum imageUrl）仅 `String()` 未过 `sanitizeUrl`，其中 forum imageUrl 作可点击外链 → 存储型 XSS/钓鱼面（比后台字段更优先）。
  - M20：`RedisCache.clear()` 是 no-op，但 `vndb.ts:688` 调它刷新缓存 → 生产 Redis 部署下 VNDB 刷新永不失效。
- 修正 L9：回源确认 `updateRole`/`delete` 均显式禁止修改 SUPER_ADMIN → "降级最后一名超管"不可触发，原判定不成立，下调为非问题。
- 已核查非缺陷留痕：4 处 `target=_blank` 均有 rel=noopener；`updatePost` 有归属校验（无 IDOR）；会话 cookie sameSite=lax 防 CSRF；硬编码 hex 多为 SVG/品牌色非主题穿透。
- 仍只读，未改源码。
